# baselines/ — classical controllers for comparison

PID / LQR / adaptive (and later MRAC-style) attitude controllers evaluated on the
same `simulation` env as the RL and MPC approaches. These provide the "classical"
reference points the proposal and thesis compare against.

_Scaffolding — nothing implemented yet._
