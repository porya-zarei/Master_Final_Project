# world_models/ — Phase 2: JEPA-style latent world model

Offline-trained latent world model (encoder + latent predictor) used as the
dynamics model for MPC planning. **Frozen** at test time here — this is
**Baseline B** (world-model MPC without adaptation).

_Scaffolding — nothing implemented yet._
